import React from "react";
import { View } from "react-native";
import PickupTimeSlash from "../components/PickupTimeSlash";

const PickupTimeScreen = () => {
  return (
    <View style={{ flex: 1 }}>
      <PickupTimeSlash />
    </View>
  );
};

export default PickupTimeScreen;
